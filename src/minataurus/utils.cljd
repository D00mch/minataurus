(ns minataurus.utils
  (:require ["package:logger/logger.dart" :as logger]))

(defn build-logger [tag]
  (logger/Logger.
    :printer (logger/PrefixPrinter.
               (logger/SimplePrinter.)
               :debug tag
               :verbose tag
               :info tag
               :warning tag
               :error tag)))
